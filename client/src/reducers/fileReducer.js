
const defaultState = {
  files: [],
  currentDir: null,
  popupDisplay: 'none',
  dirStack: [],
  view: 'list'
}

export const fileLoadReducer = (state = defaultState, action) => {
  switch (action.type) {

    default:
      return state
  }
}